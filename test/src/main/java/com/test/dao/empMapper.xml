<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.test.dao.IEmpDao">

   <!--<insert id="savaUser" parameterType="UserBean">
       insert into sys_user(userName,age) values
       (#{userName},#{age})
   </insert>

    <select id="findUserList" resultType="UserBean">
        select Id as userId,userName,age from sys_user
    </select>-->

</mapper>